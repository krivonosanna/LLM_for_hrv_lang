# dvc.yaml
stages:
  train:
    cmd: python train.py --config config.yaml data.num_samples=1000 tokenizer.num_samples_for_tokenizer=1000 trainer.n_steps=5 trainer.val_every_n_steps=3 trainer.plot_every_n_steps=1
    deps:
      - data/
      - train.py
    outs:
      - model/
      - tokenizer/
